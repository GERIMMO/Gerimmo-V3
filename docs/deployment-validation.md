# Validation deploiement

Ce fichier sert a tracer les validations de deploiement GERIMMO V3 sans modifier la logique applicative.

## Sprint 5

- Dernier objectif : aligner le deploiement public Vercel avec le dernier commit de `main`.
- Validation attendue : les routes `/dashboard/incidents` et `/dashboard/incidents/dossier` doivent etre servies par le domaine public.
